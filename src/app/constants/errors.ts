import { APP_NAME } from './general';
export const TYPE_NOT_SUPPORTED: string = `${ APP_NAME } only supports CSS files.`;
export const CSS_PARSE_ERROR: string = 'We have encountered a problem while parsing your CSS.\
                                      Please check the code integrity and try again.';